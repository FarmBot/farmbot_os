defmodule Farmbot.AssetMonitorTest do
  use ExUnit.Case

  describe "persistent regimens" do
    test "adding a persistent regimen starts a process"
    test "removing a perrsisten regimen stops a process"
  end
end
