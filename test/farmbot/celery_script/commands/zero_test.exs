defmodule Farmbot.CeleryScript.Command.ZeroTest do
  use ExUnit.Case
  alias Farmbot.CeleryScript.Ast
  alias Farmbot.CeleryScript.Command

  test "the truth" do
    # TODO(Connor) fix the truth in Zero
    assert true == false
  end
end
