defmodule FarmbotOS.SysCalls.PointLookupTest do
  use ExUnit.Case
  use Mimic
  setup :verify_on_exit!
  # alias FarmbotOS.SysCalls.PointLookup
  # alias FarmbotOS.SysCalls.ResourceUpdate

  # test "" do
  #   first_point = ResourceUpdate.resource_update("Plant", 24, %{})
  #   same_point = PointLookup.point("GenericPointer", 24)
  #   assert first_point == same_point
  # end
end
