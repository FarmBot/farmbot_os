defmodule FarmbotExt.AMQP.TerminalChannelTest do
  use ExUnit.Case, async: false
  use Mimic
  setup :verify_on_exit!
  setup :set_mimic_global

  test "terminal channel" do
    raise "TODO"
  end
end
